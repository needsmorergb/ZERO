{
  "name": "solana-paper-trader-extension",
  "version": "1.11.14",
  "description": "Paper trading overlay for Solana spot on Axiom and Terminal (Padre)",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/content.boot.js --bundle --outfile=src/content.bundle.js --format=iife",
    "watch": "esbuild src/content.boot.js --bundle --outfile=src/content.bundle.js --format=iife --watch",
    "lint": "eslint src/**/*.js",
    "format": "prettier --write \"src/**/*.{js,css,html}\""
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "eslint": "^8.50.0",
    "prettier": "^3.0.0"
  },
  "keywords": [
    "chrome-extension",
    "solana",
    "paper-trading",
    "trading",
    "cryptocurrency"
  ],
  "author": "",
  "repository": {
    "type": "git",
    "url": ""
  }
}
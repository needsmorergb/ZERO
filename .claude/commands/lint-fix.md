Run linting and formatting across the project:

1. Run `npm run lint` and capture all warnings/errors
2. Run `npm run format` to auto-fix formatting
3. If lint errors remain, fix them one by one
4. Run `npm run build` to verify fixes don't break the build
5. Report what was fixed
